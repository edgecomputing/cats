@using Cats.Helpers
@model List<Cats.Areas.Procurement.Models.RevertRequsitionsViewModel>
@{
    ViewBag.Title = "RevertRequsitions";
    const string pageName = "Procurement.TransportOrder.RevertRequsitions";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">@Html.Translate2(pageName,"List of Requsitions to be reverted")</h3>
</div>

@using (Html.BeginForm("RevertRequsitions", "TransportOrder", FormMethod.Post))
{    
   <div style="height: 400px; overflow-y: scroll;">
    <table  class="table table-striped table-bordered k-scrollable" >
        
        <thead>
            <th>Action</th>
            <th> No.</th>
            @* <th>TransporterID</th>*@
            <th>Requsition No</th>
            <th>Zone</th>
          
        </thead>
        <tbody>
      @*  @{ int index = 0; }*@
            @for (int i = 0; i < Model.Count;i++ )
            {
                
                
                
                //var id = item.Checked;
                 <tr>
                     @{var sno = i + 1;}
                     <td>@Html.CheckBox("revertRequsitionsViewModels[" + @i + "].Checked", 
                                    Model[i].Checked)</td>
                     <td>@sno</td>
                     @Html.Hidden("revertRequsitionsViewModels[" + @i + "].RequsitionID", 
                                       Model[i].RequsitionID, 
                                       new { @readonly = "readonly" })
                       <td>@Html.Label("revertRequsitionsViewModels[" + @i + "].RequsitionNumber", 
                                      Model[i].RequsitionNumber, 
                                      new { @readonly = "readonly" })</td>
                     <td>@Html.Label("revertRequsitionsViewModels[" + @i + "].Zone", 
                                       Model[i].Zone, 
                                       new { @readonly = "readonly" })</td>
                  
                       </tr>

                    @* @Html.HiddenFor(model=>item.TransportOrderID)
                     <td>@item.TransportOrderID</td>
                     @Html.HiddenFor(model=>item.ContractNumber)
                     <td>@item.ContractNumber</td>
                     @Html.HiddenFor(model=>item.TransportOrderNo)
                     <td>@item.TransportOrderNo</td>
                     @Html.HiddenFor(model=>item.Transporter)
                     <td>@item.Transporter</td>
                     @Html.HiddenFor(model=>item.Checked)*@
                   
               
            }
        </tbody>
        
    </table>
      </div>@* 
    <tr>*@
           @* <td colspan="3">*@
            <div>
                <button class="btn btn-primary"  type="submit">Revert</button>
                <a class="btn btn-primary"  data-dismiss="modal" aria-hidden="true" >Cancel</a>
       
            </div> 
           @*     </td>
        </tr>*@
}
