@model int
@using Kendo.Mvc.UI
@using Cats.Helpers

<style>
    .action-link
    {
        display: none;
    }
    .assign-code3
    {
        display: block;
    }
</style>
@(Html.Kendo().Grid<Cats.Models.PaymentRequestViewModel>()
    .Name("PaymentRequests")
    .Columns(columns =>
    {
        columns.Bound(e => e.PaymentRequestID).Width(120).Title("Payment Request");
    })
    .DataSource(dataSource => dataSource
        .Ajax()
        .Read(read => read.Action("OutstandingDeliveryNote_Read", "ContractAdministration",new {ID = 1}))
     )
     .Scrollable(scroll => scroll.Height(350))
     .Selectable(select => select.Mode(GridSelectionMode.Single))
     .Navigatable()
     .Sortable()
     .HtmlAttributes(new { style = "height:350px;" })
)
