@model int
@using Kendo.Mvc.UI
@using Cats.Helpers

<style>
    .action-link
    {
        display: none;
    }
    .assign-code3
    {
        display: block;
    }
</style>
@(Html.Kendo().Grid<Cats.Models.FDPViewModel>()
    .Name("FDPsinCurrentContract")
    .Columns(columns =>
    {
        columns.Bound(e => e.FDPID).Width(120).Title("FDP ID");
    })
    .DataSource(dataSource => dataSource
        .Ajax()
        .Read(read => read.Action("FDPsInCurrentContract_Read", "ContractAdministration",new {TransporterID = 1}))
     )
     .Scrollable(scroll => scroll.Height(350))
     .Selectable(select => select.Mode(GridSelectionMode.Single))
     .Navigatable()
     .Sortable()
     .HtmlAttributes(new { style = "height:350px;" })
)
