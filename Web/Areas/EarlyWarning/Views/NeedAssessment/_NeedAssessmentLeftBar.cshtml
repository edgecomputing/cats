@using System.Collections
<ul class="nav nav-list">
    <li class="nav-header">Region:  @ViewData["RegionName"]</li>

    @foreach (var item in ViewBag.Zones)
    {
        int id = @item.AdminUnitID;
        <li><a href="#" OnClick="zoneChange(@id);"> @item.Name </a></li>
    }
   
    
  
</ul>