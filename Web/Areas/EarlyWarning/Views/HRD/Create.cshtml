
@using Kendo.Mvc.UI
@model Cats.Models.HRD

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_MainLayout.cshtml";  
}
@section LeftBar
{
   
    @Html.Partial("_HRDLeftBar");
   
}
<div class="well"><h3>Create HRD</h3></div>
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)

        <div class="well form-horizontal">
            <div class="control-group">
                <div class="control-label">
                    @Html.LabelFor(m => m.Year)
                </div>
                <div class="controls">
                    @Html.TextBox("Year")
                    @Html.ValidationMessageFor(m=>m.Year)
                </div>
        
            </div>
            <div class="control-group">
                <div class="control-label">
                    @Html.LabelFor(m => m.Month)
                </div>
                <div class="controls">
                    @Html.DropDownList("Month",String.Empty)
                    @Html.ValidationMessageFor(m=>m.Month)
                </div>
        
            </div>
             <div class="control-group">
                <div class="control-label">
                    @Html.LabelFor(m => m.RationID,"Ration")
                </div>
                <div class="controls">
                    @Html.DropDownList("RationID",String.Empty)
                    @Html.ValidationMessageFor(m=>m.RationID)
                </div>
        
            </div>
             <div class="control-group">
                <div class="control-label">
                    @Html.LabelFor(m => m.CreatedDate,"Created Date")
                </div>
                <div class="controls">
                     <input class="cats-datepicker" type="text" id="create" name="create" />
                    @Html.HiddenFor(m=>m.CreatedDate)
                    @Html.ValidationMessageFor(m => m.CreatedDate)
                </div>
        
            </div>
            <div class="control-group">
                <div class="control-label">
                    @Html.LabelFor(m => m.PublishedDate,"Published Date")
                </div>
                <div class="controls">
                     <input class="cats-datepicker" type="text" id="published" name="published" />
                    @Html.HiddenFor(m=>m.PublishedDate)
                    @Html.ValidationMessageFor(m=>m.PublishedDate)
                </div>
        
            </div>
            <div class="control-group">      
                <div class="controls">
                    <input type="submit" value="Create HRD" class="btn btn-success" />
                </div>
            </div>

        </div>
    }
 @*@(Html.Kendo().Grid<Cats.Models.ViewModels.HRD.DetailViewModel>()
    .Name("HRD")
    .Columns(columns =>
    {
        columns.Bound(p => p.Region).Groupable(false);
        columns.Bound(p => p.Zone);
        columns.Bound(p => p.Woreda);
        columns.Bound(p => p.NumberOfBeneficiaries);
        columns.Bound(p => p.DurationOfAssistance);
        columns.Bound(p => p.StartingMonth);
        columns.Bound(p => p.Cereal);
        columns.Bound(p => p.Pulse);
        columns.Bound(p => p.CSB);
        columns.Bound(p => p.Oil);
    })
    .Pageable()
    .Sortable()
    .Scrollable()
    .Filterable()
    //.DataSource(dataSource => dataSource
          //.Server()
         // .PageSize(5)
            //.Model(model => model.Id(p => p.BidID))
            //.Read(read => read.Action("Creaet", "HRD"))
   
    )*@


