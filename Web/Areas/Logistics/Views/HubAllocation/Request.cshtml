@model IEnumerable<Cats.Models.ReliefRequisitionDetail>
@using GridMvc.Html
@{
    ViewBag.Title = "Request";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@Html.Grid(Model).Columns(columns =>
           {
                 columns.Add(foo => foo.ReliefRequisition.RequisitionNo).SetWidth(110);
                 columns.Add(foo => foo.Commodity.Name).Sortable(true);
                 columns.Add(foo => foo.Amount).Sortable(true);
        
           }).WithPaging(20)

<h1>Grid.MVC</h1>
    <hr />
    <div style="width:500px;">
        @Html.Grid(Model).Columns(columns => 
                    {
                        columns.Add(c => c.Commodity.Name);
                        columns.Add(c => c.Amount)Filterable(true);
                        columns.Add(c => c.ReliefRequisition.RequisitionNo).SetFilterWidgetType("a");
                    }).WithPaging(3).Sortable(true)
    </div>

