@model Cats.Models.Hubs.UserRoleModel
           

<div>
         @Html.HiddenFor(modelItem => modelItem.RoleId,"name=kummsa")
         @Html.CheckBoxFor(modelItem => modelItem.Selected)
        @Html.Label(Model.RoleName)
        </div>

