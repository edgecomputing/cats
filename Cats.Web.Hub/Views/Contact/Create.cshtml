@model Cats.Models.Hubs.Contact
@using Cats.Web.Hub.Helpers
@{
    ViewBag.Title = Html.Translate("Create");
}

<h2>@Html.Translate("Contacts for") @ViewBag.FDPName (FDP)</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm())
    {
    @Html.ValidationSummary(true)

        <h2>@Html.Translate("Contact")</h2>

        <div class="editor-label">
            @Html.Translate("First Name")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirstName)
            @Html.ValidationMessageFor(model => model.FirstName)
        </div>

        <div class="editor-label">
            @Html.Translate("Last Name")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LastName)
            @Html.ValidationMessageFor(model => model.LastName)
        </div>

        <div class="editor-label">
            @Html.Translate("Phone Number")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PhoneNo)
            @Html.ValidationMessageFor(model => model.PhoneNo)
        </div>

        
            @Html.HiddenFor(model => model.FDPID)
     <div class="clear"></div>   

        <div style="clear:both">
            <input type="submit" value="@Html.Translate("Create")" />
        </div>
    
    }

<div>
    @Html.ActionLink(Html.Translate("Back to List"), "Index", new { fdpId = Convert.ToInt32(ViewBag.FDPID) })
</div>
